const classes = {
  activeClass: 'active',
  inActiveClass: 'inactive',
  disableClass: 'disable',
  showClass: 'show',
  hideClass: 'hide',
  errorClass: 'error',
  emptyClass: 'empty',
  animateClass: 'animate',
  bodyClass: 'body'
};

// const selectors = {
// };

const jQueryObjects = {
  _window: $(window),
  _document: $(document)
};

export {
  classes,
  // selectors,
  jQueryObjects
};
